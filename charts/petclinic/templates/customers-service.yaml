apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers-service
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customers-service
  template:
    metadata:
      labels:
        app: customers-service
    spec:
      containers:
        - name: customers-service
          image: {{ .Values.customers_service.image }}
          ports:
            - containerPort: 8081
          resources:
            {{- toYaml .Values.customers_service.resources | nindent 12 }}
          env:
            - name: EUREKA_INSTANCE_HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: customers-service
  namespace: {{ .Values.namespace }}
  labels:
    app: customers-service
spec:
  selector:
    app: customers-service
  ports:
    - protocol: TCP
      port: {{ .Values.customers_service.service.port }}
      targetPort: 8081
  type: {{ .Values.customers_service.service.type }}